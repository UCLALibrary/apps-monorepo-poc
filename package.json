{
  "name": "stellar",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "vue-component-library": "pnpm -r --filter vue-component-library run build",
    "nuxt:prepare": "pnpm -r --filter @stellar/nuxt run dev:prepare",
    "nuxt:dev": "pnpm -r --filter @stellar/nuxt run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "catalog:",
    "vite": "catalog:",
    "vue-tsc": "catalog:"
  },
  "engines": {
    "pnpm": "^9.12.1"
  }
}
